<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE TS>
<TS version="2.1" language="de">
<!--
  Collaborative Layout Designer translation catalogue for 'de'.

  To populate this file, run from the repo root:

      lupdate -recursive src -ts translations/cld_de.ts

  That pulls every tr() call out of the source tree and adds a
  <message> entry per unique string. Translators then use Qt Linguist
  (or a plain text editor) to fill in the <translation> body of each
  entry, leaving the <source> untouched.

  Once translated, compile via lrelease (run automatically by CMake on
  next build). See docs/TRANSLATING.md for the full workflow.
-->
<context>
    <name>CollaborativeLayoutDesigner</name>
    <!-- Translation messages go here once lupdate has populated them. -->
</context>
</TS>
